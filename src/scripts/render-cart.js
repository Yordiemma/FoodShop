import { cartItems } from "./cart";

export const totalItemsEl = document.querySelectorAll('.total-items')
const totalPriceEl = document.getElementById('total-price')

totalPriceEl.textContent = 10

const totalPrice = () => {
    
}

// console.log(cartItems)
